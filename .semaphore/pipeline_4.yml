version: v1.0
name: 'Deploy ${{parameters.ENVIRONMENT}}'
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Apply
    task:
      jobs:
        - name: Terraform Apply
          commands:
            - echo "job 1"
